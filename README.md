# fake-news-detection

If activiating virtual environments is not allowed, run the following"
```
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
```

Also, make sure after installing requirements, you install:
```
pip uninstall torch
pip install torch --index-url https://download.pytorch.org/whl/cu118
```